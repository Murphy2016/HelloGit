架构思维
ibatis，nosql，mongodb，
JDK源码，
spring,
缓存、消息、大数据存储等相关技术，例如：redis、memcached、zookeeper、ActiveMQ、dubbo、elasticsearch、mongodb、hbase等；
优秀开源软件研究，
数据库优化
NIO,RPC框架，
JVM原理，调优
CAP、Quorum、一致性Hash、Paxos
熟悉系统底层开发接口，精通Java多线程，Socket通信编程
向量时钟、paxos、NWR一致性分布式算法
常用降级方案（消息中间消峰、梯度限流、最少关键数据）
缓存(Redis/Memached))、消息(Kafka/RabbitMQ/ActiveMQ),
RabitMQ/ActiveMQ, Kafka/Jafka，Hadoop/Hive
Maven, Gradle
熟悉常规的xmpp等通讯框架，熟悉activesync等同步协议
java反射,Jboss
Scala,大数据（hadoop，spark，storm等）hbase，hive，kafka/storm/spark等流式数据处理平台
反向代理，负载均衡
Linux,shell,python,
分布式存储（fastDFS、HDFS等）
基于流式计算框架storm、swark、genfire、eswer（CEw）
HttpClient，Jsoup
Velocity
常见Servlet容器（Tomcat/Resin等）
github/gitlab, jenkins，docker
Spark,Flink等大数据平台
熟悉 MySQL 的应用开发、常见的性能调优和可扩展性设计，熟悉事务、锁、并发等机制；
